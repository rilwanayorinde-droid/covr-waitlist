{
  "name": "covr-waitlist",
  "version": "1.0.0",
  "description": "COVR Waitlist — Standalone",
  "scripts": {
    "build": "echo 'Static site - no build needed'"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
